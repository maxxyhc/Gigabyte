[project]
name = "aorus-spec-rag"
version = "0.1.0"
description = "Hand-written RAG QA system for the GIGABYTE AORUS MASTER 16 AM6H spec page, running within 4GB VRAM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "numpy>=2.5.2",
    "requests>=2.34.2",
    "sentence-transformers>=6.0.0",
]
